class VisitsController < ApplicationController

  def create
    # Every time a visitor visits a shortened link - they've just "created" a visit
    # Hint: Take advantage of wildcard url segments to make this feature work!
  end

end
